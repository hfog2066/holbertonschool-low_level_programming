#include <stdio.h>
/**
 *main -Print a text string using printf function
 *
 *Description: fuction show "with proper grammar, but the
 *outcome is a piece of art" through printf function.
 *Return: 0 at the end of the routine
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
