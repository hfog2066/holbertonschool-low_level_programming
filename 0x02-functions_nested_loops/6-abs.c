#include "holberton.h"
/**
 *main - show absolute value
 *
 *Description: prints absolute value of
 *a integer.
 *
 *Return: 0
 */
int _abs(int);
{
if (n <= 0)
return (n - (n * 2));
else
return (n);
}
