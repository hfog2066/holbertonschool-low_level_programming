#include "holberton.h"
#include <stdio.h>
/**
 *main - show absolute value
 *
 *Description: prints absolute value of
 *a integer.
 *
 *Return: 0
 */
int _abs(int)
{
int i;
if (i < 0)
{
i = i * -1;
return (i);
}
else
{
return (i);
}
}
